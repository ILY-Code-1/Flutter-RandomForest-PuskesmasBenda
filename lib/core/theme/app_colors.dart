/// Definisi warna untuk aplikasi Sistem Antrian Puskesmas
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Colors - Hijau
  static const Color primaryGreen = Color(0xFF1B5E20);      // Hijau tua untuk tombol & teks utama
  static const Color primaryGreenLight = Color(0xFF4CAF50); // Hijau medium
  static const Color backgroundGreen = Color(0xFFE8F5E9);   // Hijau muda untuk background
  static const Color accentGreen = Color(0xFF81C784);       // Hijau aksen

  // Accent Colors
  static const Color accentYellow = Color(0xFFFFEB3B);      // Kuning untuk gradasi wave
  static const Color accentYellowLight = Color(0xFFFFF9C4); // Kuning muda

  // Neutral Colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF212121);
  static const Color grey = Color(0xFF757575);
  static const Color greyLight = Color(0xFFBDBDBD);
  static const Color greyBackground = Color(0xFFF5F5F5);

  // Card & Shadow
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color shadowColor = Color(0x1A000000);       // 10% opacity black

  // Text Colors
  static const Color textPrimary = Color(0xFF1B5E20);       // Hijau tua
  static const Color textSecondary = Color(0xFF424242);
  static const Color textHint = Color(0xFF9E9E9E);
}
